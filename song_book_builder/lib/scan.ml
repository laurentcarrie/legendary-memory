(* module Log = Dolog.Log *)

let scan_ly sheet filename =
  let _ = sheet in
  let _ = filename in
  (*  let path = sheet.Sheet.tmpdir ^ "/" ^ filename in *)
  (*  let data = In_channel.with_open_text path In_channel.input_all in *)
  (*  Log.info "%s" data; *)
  ""
